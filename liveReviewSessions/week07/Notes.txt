Statements in Java
    * Decision Making Statements (Conditionals)
        - if, if-else, switch, nested statements, ternary
    * Looping Statements
        - for loops, while, do while, nested loops
    * Branching statements:
        - break: this one breaks the loop and switch
        - continue: this one skips the following steps in the loop
        - return : will exit the main method
for loop & while and do while
initialization
condition
iteration: should be capable enough to change/eeffect the condition

while loops: acts like repeated if condition

Unicodes

Random Class from Java
Step 1:
 Random random = new Random();  // create random object
Step 2:
   to create random integer numbers
   int number = random.nextInt(upperbound)

